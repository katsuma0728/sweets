<h1>
  <span><%= @order.customer.last_name + @order.customer.first_name %></span>さんの注文履歴
</h1>

<table class = "orders">
  <thead>
    <tr>
      <th>購入日時</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% @order.each do |order| %>
        <td><%= link_to order.created_at, admin_order_path(order.id) %></td>
        <td><%= order.order_items.amount %></td>
        <td><%= order.order_status %></td>
      <% end %>
    </tr>
  </tbody>
</table>